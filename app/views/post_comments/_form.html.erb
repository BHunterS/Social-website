<%= form_with(model: post_comment) do |form| %>
  <% if post_comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post_comment.errors.count, "error") %> prohibited this post_comment from being saved:</h2>

      <ul>
        <% post_comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form">
  <div class="form__content">
    <%= form.label :post, style: "display: block", class: "form__label" %>
    <%= form.text_field :content, class: "form__field" %>
  </div>
  <div class="form__content">
    <%= form.label :profile, style: "display: block", class: "form__label" %>
    <%= form.text_field :content, class: "form__field" %>
  </div>
  <div class="form__content">
    <%= form.label :text, style: "display: block", class: "form__label" %>
    <%= form.text_field :content, class: "form__field" %>
  </div>

  <div>
    <%= form.submit class: "form__button" %>
  </div>
</div>
<% end %>
